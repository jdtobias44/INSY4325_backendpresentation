services:
  - type: web
    name: marketplace-backend
    runtime: java
    buildCommand: mvn clean package -DskipTests
    startCommand: java -jar target/marketplace.jar
    envVars:
      - key: PORT
        value: 8080
